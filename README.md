## How to run locally
In the project directory,
### `npm install`
### `npm run express`

## How to run with Docker
In the project directory,
### `npm run docker:build`
### `npm run docker:run`

## Available endpoints
### `http://localhost:8080/`
### `http://localhost:8080/api/team`
